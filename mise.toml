[tools]
yamlfmt = "0.21.0"
tombi = "1.2.4"
"aqua:numtide/treefmt" = "2.5.0"
"cargo:flip-link" = "0.1.12"
"cargo:cargo-binutils" = "0.4.0"
"cargo:cargo-hex-to-uf2" = "0.1.2"

[tasks.build]
description = "Build central and peripheral firmware"
run = "cargo build --release"

[tasks.hex]
description = "Generate Intel HEX images"
depends = ["build"]
run = [
  "cargo objcopy --release --bin central -- -O ihex cornix-rmk-central.hex",
  "cargo objcopy --release --bin peripheral -- -O ihex cornix-rmk-peripheral.hex",
]

[tasks.uf2]
description = "Generate uf2 images for the Adafruit nRF52 bootloader"
depends = ["hex"]
run = [
  "cargo hex-to-uf2 --input-path cornix-rmk-central.hex --output-path cornix-rmk-central.uf2 --family nrf52840",
  "cargo hex-to-uf2 --input-path cornix-rmk-peripheral.hex --output-path cornix-rmk-peripheral.uf2 --family nrf52840",
]

[tasks.lint]
description = "Run clippy linter"
run = "cargo clippy --all-features -- -D warnings"

[tasks.fmt]
description = "Run treefmt"
run = "treefmt --tree-root . --walk filesystem --fail-on-change"
